---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: rocketchat-netpol
  namespace: sbos-comms
spec:
  podSelector:
    matchLabels:
      app: rocketchat
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              sbos-role: internal

      ports:
        - protocol: TCP
          port: 3000
