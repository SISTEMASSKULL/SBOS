---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cert-rotation-netpol
  namespace: sbos-installer
spec:
  podSelector:
    matchLabels:
      app: cert-rotation
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              sbos-role: internal

      ports:
        - protocol: TCP
          port: 80
