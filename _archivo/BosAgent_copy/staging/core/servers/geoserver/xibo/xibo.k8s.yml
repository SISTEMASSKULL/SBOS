---
apiVersion: v1
kind: Service
metadata:
  name: xibo
  namespace: sbos-geo
spec:
  type: ClusterIP
  selector:
    app: xibo
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xibo
  namespace: sbos-geo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: xibo
  template:
    metadata:
      labels:
        app: xibo
    spec:
      serviceAccountName: xibo-sa
      containers:
        - name: xibo
          image: docker.io/xibosignage/xibo-cms:4.1
          ports:
            - containerPort: 8080
              name: http

          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10


