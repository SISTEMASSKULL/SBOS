# Manual de Completitud del Desarrollo — BOS Control Plane Soberano
## Norte: IAM Enterprise · Orden: manuales ①–⑥ · Verificación: VPS real

**Versión:** 2.0.0
**Fecha:** 2026-07-31
**Autor:** bos-developer — SBOS
**Carta rectora:** [0.00 — Directrices BOS Control Plane](0.00_MANUAL-DIRECTRICES-BOS-CONTROL-PLANE.md)
**Estado:** 🟡 BORRADOR — sujeto a revisión del operador

---

## Lectura obligatoria antes de usar este plan

Este plan NO es un backlog de tareas. Es un **mapa de madurez** estructurado según los
6 motores del corpus documental. El orden de los motores define el orden del desarrollo:
no se puede observar lo que no está instalado (① antes que ②), no se puede administrar fichas
sin observabilidad (② antes que ③), no se puede resolver contexto sin fichas instaladas
(③ antes que ④), no se puede certificar sin contexto (④ antes que ⑤-⑥).

**El norte es IAM Enterprise.** Eso significa: FAPI 2.0, NIST SP 800-207 Zero Trust,
ISO 27001:2022 A.8.x, OAuth 2.1, OIDC, SCIM, mTLS, SLOs verificados.
Cada fase de este plan avanza hacia ese estándar.

**Principio de verificación:** una fase está completa cuando el operador puede ejecutar
los comandos de verificación en la VPS y obtener el resultado esperado — sin leer el código.

---

## Estado de partida honesto (2026-07-31)

| Componente | Estado real |
|-----------|------------|
| Go build `./...` | ✅ limpio |
| Motor ③ Server FICHAS | ✅ L3 — código completo, tests, funcional en aislamiento |
| Motor ① IAM Installer (saga, compensator, pg_auxiliar) | 🟡 L2 — código presente, saga pasos 1-2 OK, paso 3+ bloqueados sin VPS |
| Motor ④ Context Plane (service, store, types, unified) | 🟡 L2 — 11 métodos implementados, NO operativo sin PG+Redis |
| Motor ⑤ Dashboard (55 métodos registrados) | 🟡 L2 — 5 métodos `bos.web.domain.*` documentados pero sin código |
| Motor ② SO Observable — 4 motores Capacidad | ❌ L0 — collector/forecaster/policy/action: ningún archivo existe |
| Motor ⑥ Banco de Pruebas | ❌ L0 — estructura del 6.01 existe, 0 pruebas ejecutadas contra daemon real |
| Stack Alpha en VPS (PG+Redis+Vault+KC+Kong) | ❌ no instalado — bloqueante de todo |
| Port Manager en lifecycle (Assign/Release) | ❌ código existe, no conectado a lifecycle.go |
| internal/certman/ | ❌ directorio no existe |
| internal/eventbus/ (Redis Streams) | ❌ directorio no existe |
| internal/capacity/collector+forecaster+policy+action | ❌ archivos no existen |
| Fichas S03 (KC/Vault/Kong/bAuth) | ❌ directorios vacíos |
| Zero Trust: NetworkPolicy, mTLS, Wazuh | ❌ sin código |
| FAPI 2.0, ISO 27001 evidencia, SLSA L2 | ❌ sin ejecutar |

---

## Manuales faltantes identificados

El corpus documental tiene 27 manuales + 13 anexos. Los siguientes **subsistemas con código
sustancial no tienen manual propio** y deben escribirse en paralelo con su implementación:

| Manual faltante | Subsistema que documenta | Fase que lo activa |
|----------------|-------------------------|--------------------|
| `1.06_MANUAL-IAM-INSTALLER-ZERO-TRUST.md` | mTLS, NetworkPolicy Calico, Wazuh SIEM, NRS-01..NRS-12 | Fase II |
| `2.05_MANUAL-SO-OBSERVABLE-CAPACIDAD-MOTORES.md` | Los 4 motores Day 2 (collector/forecaster/policy/action) | Fase V |
| `3.09_MANUAL-SERVER-FICHAS-EVENTBUS.md` | Redis Streams event bus, contratos de eventos entre daemons | Fase IV |
| `4.06_MANUAL-CONTEXT-PLANE-IAM-ENTERPRISE.md` | Integración ctx→bAuth 12 dominios, PEP Kong, FAPI 2.0 | Fase III |
| `5.05_MANUAL-DASHBOARD-BIAOS.md` | Agente IA soberano (`internal/biaos/`) — no tiene ningún manual | Fase VI |
| `5.06_MANUAL-DASHBOARD-SECURITY-RBAC.md` | RBAC dual FileRBAC/BauthRBAC, CIS Benchmark, scanner | Fase VI |
| `A.16_ANEXO-ZERO-TRUST-IMPLEMENTACION.md` | Guía de implementación NIST SP 800-207 en SBOS | Fase II |
| `A.17_ANEXO-FAPI2-CERTIFICACION.md` | Test suite FAPI 2.0, PAR, DPoP, PKCE contra KC | Fase VII |

---

## FASE I — Fundación operativa: Stack Alpha en VPS

**Motor:** ① IAM Installer · **Manuales:** 1.01 · 1.02 · A.11
**Estándar IAM Enterprise:** base para todo — sin stack no hay IAM
**Bloqueante de:** todas las fases siguientes

### Por qué primero

El manual 1.01 §4 define las 6 capas del bootstrap progresivo. La Capa 0 (kubeadm) y la
Capa 1 (PG+Redis) son los cimientos físicos. Sin ellas, los 11 métodos del Context Plane,
los 55 métodos JSON-RPC y los 4 motores de capacidad son código que no puede ejecutarse.
El REGISTRO-ESTADO confirma que M2.2 (Stack Alpha) es el único átomo con 30+ dependientes.

### Tareas de implementación

**Fichas S01-dataserver (manor.1.01 §11, A.11 Paso 7-15)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| I.1 | Ficha `postgresql` completa: manifest.yml + task_catalog.sh (install/health/repair/remove idempotente) + PROPOSITO.md + resources/init.sql | `servers/S01/postgresql/` | `bosctl ficha status postgresql` → INSTALADA |
| I.2 | Ficha `redis` completa: manifest.yml + task_catalog.sh + PROPOSITO.md | `servers/S01/redis/` | `bosctl ficha status redis` → INSTALADA |
| I.3 | Ficha `minio` completa: manifest.yml + task_catalog.sh + PROPOSITO.md | `servers/S01/minio/` | `bosctl ficha status minio` → INSTALADA |

**Fichas S02-gatewayserver (A.11 Paso 16-22)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| I.4 | Ficha `vault` completa: manifest.yml + task_catalog.sh + resources/vault-init.sh + PROPOSITO.md | `servers/S02/vault/` | `bosctl ficha status vault` → INSTALADA |
| I.5 | Ficha `kong` completa: manifest.yml + task_catalog.sh + resources/kong-declarative.yml + PROPOSITO.md | `servers/S02/kong/` | `bosctl ficha status kong` → INSTALADA |
| I.6 | Ficha `nginx` completa: manifest.yml + task_catalog.sh + resources/nginx.conf.tmpl | `servers/S02/nginx/` | `bosctl ficha status nginx` → INSTALADA |

**Fichas S03-identityserver (A.11 Paso 23-30)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| I.7 | Ficha `keycloak` completa: manifest.yml + task_catalog.sh + resources/realm-bootstrap.json + PROPOSITO.md | `servers/S03/keycloak/` | `bosctl ficha status keycloak` → INSTALADA · realm `skull` en KC |
| I.8 | Ficha `bauth` completa: manifest.yml + task_catalog.sh + PROPOSITO.md + resources/bauth.service | `servers/S-HOST/bauth/` | `bosctl ficha status bauth` → INSTALADA · `/run/bos/bauth.sock` existe |

**Saga de instalación (1.02 — saga 7 pasos con compensación)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| I.9 | Saga `bosctl deploy` completa: 7 pasos con compensación funcionales en VPS real. Pasada 1 (mínimo viable sin Vault) + Pasada 2 (credenciales a Vault) | `internal/installer/saga.go` · `cmd/bosctl/deploy.go` | `bosctl deploy --seed seed-skull.yml` → saga completa sin errores |
| I.10 | `bosctl bootstrap verify` retorna estado real C-01..C-08 de VPS (no stubs) | `cmd/bosctl/bootstrap.go` | `bosctl bootstrap verify` → `8/8 ✓` |

**Ciclo de vida de Tenants (1.03)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| I.11 | `bosctl tenant create`: realm KC + namespace K8s + BD PG + paths Vault en saga con compensación | `internal/installer/` · `cmd/bosctl/` | `bosctl tenant create --id empresa1` < 30s · 4 componentes activos |
| I.12 | `bosctl tenant suspend/remove/list`: ciclo completo (suspend ≠ remove — soft-delete) | `cmd/bosctl/` | `bosctl tenant list` → skull [active] · empresa1 [suspended] |
| I.13 | `bos.tenant.*` JSON-RPC: create, remove, suspend, list, status — 5 métodos | `internal/server/jsonrpc.go` | `bos_rpc bos.tenant.list` → array de tenants con estado |

**Escritura del manual faltante (en paralelo con I.7-I.8):**
Escribir `1.06_MANUAL-IAM-INSTALLER-ZERO-TRUST.md` — estructura y hallazgos de NRS-01..NRS-12,
política de mTLS, NetworkPolicy. Puede ser v0.1 durante esta fase — se completa en Fase II.

### Verificación de la Fase I

```bash
# El comando definitivo de la Fase I
bosctl bootstrap verify
# Resultado esperado:
# C-01 kubeadm K8s      ✓   C-02 PostgreSQL 18.4   ✓
# C-03 Redis 8.6.2      ✓   C-04 Vault 2.0.1        ✓
# C-05 Keycloak 26.6.2  ✓   C-06 Kong 3.9.x LTS     ✓
# C-07 bAuth socket     ✓   C-08 NetworkPolicies     ✓
# 8/8 criterios ✓

bosctl ficha list
# postgresql INSTALADA · redis INSTALADA · vault INSTALADA
# keycloak   INSTALADA · kong  INSTALADA · bauth INSTALADA

bosctl tenant create --id empresa1
# real   0m27s   → < 30s P50 ✓
```

---

## FASE II — Seguridad Zero Trust

**Motor:** ① IAM Installer (1.04 NRS) · **Manuales:** 1.04 · 1.05 · A.15 (parcial)
**Estándar IAM Enterprise:** NIST SP 800-207 · ISO 27001 A.8.22 · NRS-01..NRS-12
**Requiere:** Fase I completa (Vault PKI operativo)

### Por qué segundo

Zero Trust es un prerequisito de IAM Enterprise, no un agregado opcional. Sin mTLS no hay
autenticación de workload. Sin NetworkPolicy deny-all no hay aislamiento. Sin Wazuh no hay
SIEM. El manual 1.04 dice claramente que NRS-03 (mTLS), NRS-04 (deny-all) y NRS-10 (CI
gosec/govulncheck) son "❌ sin implementar". Estos tres items bloquean la certificación ISO 27001.

### Tareas de implementación

**Capa de red (1.04 NRS-01..NRS-12)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| II.1 | NetworkPolicy Calico deny-all por namespace: `servers/S01/resources/netpol-dataserver.yml` (y equivalente para S02, S03) | `servers/S01,S02,S03/resources/` | `kubectl get networkpolicy -A` → deny-all en todos los namespaces SBOS |
| II.2 | mTLS entre daemons: activar `ClientAuth = RequireAndVerifyClientCert` en api.go + certs Vault PKI para cada daemon | `cmd/bos/api.go` | `openssl s_client -connect localhost:9443` → `Verify return code: 0` |
| II.3 | TLS 1.3 obligatorio en Kong (NRS-02): `resources/kong-tls-policy.yml` + cert Vault PKI | `servers/S02/kong/resources/` | `nmap --script ssl-enum-ciphers localhost 443` → solo TLS 1.3 |
| II.4 | Wazuh SIEM con reglas SBOS (NRS-05): ficha `wazuh` en S03 + reglas para bos.audit.* | `servers/S03/wazuh/` | `bosctl ficha status wazuh` → INSTALADA · alertas SBOS visibles en Wazuh |
| II.5 | CI pipeline con gosec + govulncheck + gitleaks (NRS-10) | `.github/workflows/security.yml` | pipeline CI verde en cada push |

**CERTMAN — Kardex de certificados TLS (A.15 §2.7)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| II.6 | Crear `internal/certman/` — kardex.go (CRUD T-413), manager.go (Issue/Revoke/Check), watcher.go (goroutine 6h) | `internal/certman/` | directorio y archivos existen, compilan |
| II.7 | Wire certman en lifecycle.go: al instalar ficha TLS → `certman.Issue()` · al removerla → `certman.Revoke()` | `internal/ficha/lifecycle.go` | `bosctl cert list` muestra certs del stack |
| II.8 | Wire certman watcher en run_normal.go (subsistema #16) | `cmd/bos/run_normal.go` | log "certman watcher iniciado" al arrancar |
| II.9 | `bos.certman.*` JSON-RPC: issue, check, revoke, list | `internal/server/jsonrpc.go` | `bos_rpc bos.certman.list` → array de certs |
| II.10 | `bosctl cert list/status/export` CLI | `cmd/bosctl/security.go` | `bosctl cert list` → tabla con certs activos |

**Escribir manual:** `1.06_MANUAL-IAM-INSTALLER-ZERO-TRUST.md` v1.0 (completar) +
`A.16_ANEXO-ZERO-TRUST-IMPLEMENTACION.md` v1.0

### Verificación de la Fase II

```bash
# Zero Trust verificado
kubectl get networkpolicy -A | grep -c sbos          # ≥ 6 NetworkPolicies
bosctl cert list                                       # certs del stack en Kardex T-413
curl -k https://localhost:9443/health                 # TLS 1.3, cert válido Vault PKI

# Wazuh alertas
curl http://localhost:55000/security/events \
  -H "Authorization: Bearer <token>" | jq '.affected_items[0]'

# CI limpio
# gosec: 0 issues HIGH | govulncheck: 0 vulnerabilidades | gitleaks: 0 secretos
```

---

## FASE III — Context Plane IAM Enterprise

**Motor:** ④ Context Plane · **Manuales:** 4.01 · 4.02 · 4.03 · 4.04 · 4.05
**Estándar IAM Enterprise:** NIST SP 800-207 Policy Administrator · FAPI 2.0 · OAuth 2.1
**Requiere:** Fase I (PG+Redis+Vault+KC+bAuth operativos) + Fase II (mTLS)

### Por qué tercero

El Context Plane es el Policy Administrator del modelo NIST SP 800-207. Sin él, Kong no puede
actuar como PEP porque no tiene contexto de sesión que evaluar. Sin él, bAuth no puede hacer
evaluación de 12 dominios porque no recibe el ctx_id. El manual 4.01 dice "Estado L2 — sustrato
OK, NO operativo (TODO F5)". La Fase I provee la infraestructura que desbloquea F5 completo.

### Tareas de implementación

**Context Plane operativo con PG+Redis reales (4.01 · 4.02)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| III.1 | `context.Service.Register()` con PG real: escribe en `bos.ctx_registered_device` (T-395) — activar PGRedisStore | `internal/context/service.go` · `internal/context/store.go` | `bos_rpc bos.ctx.device.register {...}` → fila en T-395 |
| III.2 | `context.Service.Promote()` con Redis real: ctx_id en `bos.ctx_context_session` (T-396) + Redis DB1 TTL 12h | `internal/context/service.go` | `bos_rpc bos.ctx.promote {...}` → ctx_id en Redis, fila en T-396 |
| III.3 | `context.Service.Get()`: lookup Redis primero, fallback PG — P99 < 5ms objetivo | `internal/context/service.go` | `time bos_rpc bos.ctx.get {"ctx_id":"..."}` < 50ms end-to-end |
| III.4 | Audit trail WORM: toda operación ctx escribe en `bos.ctx_context_audit` (T-397, hash-chain SHA-256) | `internal/audit/` + `internal/context/service.go` | `tail /var/log/bos/audit.log` → entries con ctx_id + hash |
| III.5 | `bosctl ctx list/get/heartbeat/invalidate` funcionando contra daemon real | `cmd/bosctl/context.go` | `bosctl ctx list --tenant skull` → sesiones activas reales |

**Integración ctx → bAuth (4.05 · 4.06 nuevo manual)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| III.6 | `internal/bauth/client.go`: `EvaluateDomains(ctx_id, domainList []string)` — invoca bAuth.sock para evaluar los 12 dominios | `internal/bauth/` | `bos_rpc bos.ctx.resolve {"ctx_id":"..."}` → 12 dominios evaluados |
| III.7 | `context.Service.Resolve()`: ctx_id → llama EvaluateDomains → cachea resultado en Redis DB1 TTL 30s | `internal/context/service.go` | segundo resolve < 2ms (cache hit Redis) |
| III.8 | Plugin SBOS-Context para Kong: intercepta cada request, valida ctx_id con `bos.ctx.get`, retorna 401 si inválido | `servers/S02/kong/resources/sbos-ctx-plugin/` | `curl -H "X-CTX-ID: invalid" https://sbos.app/api` → 401 |

**FAPI 2.0 (1.05 · A.17 nuevo anexo)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| III.9 | Configurar KC realm para FAPI 2.0: PAR + DPoP + PKCE obligatorio + algoritmos PS256/ES256 | `servers/S03/keycloak/resources/realm-fapi2.json` | test suite FAPI 2.0 Conformance Tests: 0 failures |
| III.10 | Vault PKI para tokens DPoP: certs efímeros ECDSA P-256 por sesión | `servers/S02/vault/resources/pki-dpop-policy.hcl` | token JWT con `cnf.jkt` thumbprint verificable |

**Escribir manual:** `4.06_MANUAL-CONTEXT-PLANE-IAM-ENTERPRISE.md` v1.0 +
`A.17_ANEXO-FAPI2-CERTIFICACION.md` v1.0

### Verificación de la Fase III

```bash
# Context Plane real
bos_rpc '{"jsonrpc":"2.0","method":"bos.ctx.device.register",
  "params":{"hostname":"nodo-01","tenant_id":"skull"},"id":1}'
# → {"result":{"dctx_id":"<uuid>","state":"PENDIENTE"}}

PGPASSWORD=postgres psql -h localhost -p 15432 -U postgres -d SBOSDB \
  -c "SELECT COUNT(*) FROM bos.ctx_registered_device"  # > 0

# Evaluar 12 dominios
bos_rpc '{"jsonrpc":"2.0","method":"bos.ctx.resolve","params":{"ctx_id":"..."},"id":2}'
# → {"result":{"domains":{"D00":true,"D01":true,...,"D11":true}}}

# FAPI 2.0 (requiere dominio público staging)
# Resultado: 0 failures en FAPI 2.0 Conformance Test Suite
```

---

## FASE IV — Server FICHAS completo y wired

**Motor:** ③ Server FICHAS · **Manuales:** 3.01 · 3.02 · 3.03 · 3.04 · 3.06 · 3.07 · 3.08 · A.12
**Estándar IAM Enterprise:** ISO 27001 A.8.20 (inventario activos) · RFC 6335
**Requiere:** Fase I (stack operativo) — el código del motor ya existe, falta conectarlo

### Por qué cuarto

El motor ③ es el más completo en código (L3) pero le faltan dos conexiones críticas: el Port
Manager no está wired en lifecycle.go (Assign/Release no se llaman), y el event bus Redis
Streams no existe. El manual 3.08 y el anexo A.12 describen el Kardex como "inventario de activos
de red ISO 27001 A.8.20" — sin él el SBOS no cumple ese control de seguridad.

### Tareas de implementación

**Port Manager wired en ciclo de vida (3.08 · A.12)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| IV.1 | `lifecycle.go` → estado INSTALADA: llamar `portman.AssignFromManifest(fichaID, manifest)` | `internal/ficha/lifecycle.go` | instalar postgresql → `bosctl port list` muestra 5432 asignado |
| IV.2 | `lifecycle.go` → estado REMOVIDA: llamar `portman.Release(fichaID)` | `internal/ficha/lifecycle.go` | remover postgresql → 5432 en estado `released` (inmutabilidad) |
| IV.3 | `reconcile/scheduler.go`: cada 300s, `portman.ValidateKardex()` — detecta drift puertos Kardex vs K8s | `internal/reconcile/scheduler.go` | `bosctl port validate` → "Kardex consistente" o lista drift |

**Event Bus Redis Streams (3.09 nuevo manual)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| IV.4 | Crear `internal/eventbus/` — publisher.go + subscriber.go sobre Redis Streams (DB2) | `internal/eventbus/` | compila, test unitario con Redis real |
| IV.5 | Publicar eventos de ciclo de vida de ficha: `ficha.installed`, `ficha.removed`, `ficha.degraded`, `ficha.repaired` | `internal/ficha/lifecycle.go` | `redis-cli XLEN bos:events:ficha` > 0 después de instalar |
| IV.6 | Publicar eventos de ctx: `ctx.promoted`, `ctx.invalidated`, `ctx.switch` en Redis Streams DB1 | `internal/context/service.go` | `redis-cli XLEN bos:events:ctx` > 0 |

**Fichas daemons hermanos como stubs (ADR-007, bos-repair BOS-REPAIR-16)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| IV.7 | Ficha `bkernel` (S-HOST): manifest.yml + task_catalog.sh stub + PROPOSITO.md | `servers/S-HOST/bkernel/` | `bosctl ficha status bkernel` → INSTALADA |
| IV.8 | Ficha `biedata` (S-HOST): stub + contrato JSON-RPC declarado en PROPOSITO.md | `servers/S-HOST/biedata/` | `bosctl ficha list` muestra biedata |
| IV.9 | Ficha `bsearch` (S-HOST): stub + PROPOSITO.md | `servers/S-HOST/bsearch/` | `bosctl ficha list` muestra bsearch |
| IV.10 | Ficha `bnotify` (S-HOST): stub + PROPOSITO.md | `servers/S-HOST/bnotify/` | `bosctl ficha list` muestra bnotify |

**Fichas de aplicación S04-S15 (3.07 Catálogo)**

| # | Tarea | Paquete / Archivo | Prioridad |
|---|-------|-------------------|-----------|
| IV.11 | Fichas S12-monitorserver: prometheus, grafana, loki, alertmanager | `servers/S12/` | Alta — observabilidad |
| IV.12 | Fichas S04-erpserver: tryton-erp | `servers/S04/` | Alta — producto |
| IV.13 | Fichas S06-appsserver: orangehrm, espoCRM, taiga | `servers/S06/` | Media |
| IV.14 | Fichas S10-commsserver: postfix, dovecot, rocket.chat | `servers/S10/` | Media |
| IV.15 | Fichas S14-opsserver: gitlab, bareos, velero | `servers/S14/` | Media |
| IV.16 | Fichas S05, S07, S08, S09, S13, S15 | `servers/S05-S15/` | Baja |

**Escribir manual:** `3.09_MANUAL-SERVER-FICHAS-EVENTBUS.md` v1.0

### Verificación de la Fase IV

```bash
# Port Manager en acción
bosctl ficha install postgresql
bosctl port list
# postgresql  5432  TCP  HOST_PHYSICAL  assigned  <fecha>

bosctl ficha remove postgresql
bosctl port list
# postgresql  5432  TCP  HOST_PHYSICAL  released  <fecha>

# Event bus
redis-cli XLEN bos:events:ficha
# > 0

# Catálogo completo
bosctl ficha list --all | wc -l
# ≥ 50 fichas
```

---

## FASE V — Observabilidad Day 2: Autómata de Capacidad

**Motor:** ② SO Observable · **Manuales:** 2.01 · 2.02 · 2.03 · 2.04
**Estándar IAM Enterprise:** ISO 27001 A.8.16 (monitoreo) · ITIL 4 Capacity Management
**Requiere:** Fase I (PG+Redis+stack operativo para tener métricas reales)

### Por qué quinto

El manual 2.02 describe 4 motores del Autómata de Capacidad (Observación, Proyección, Políticas,
Acción). El REGISTRO-ESTADO confirma que M5.1-M5.4 son 🔴 sin ninguna línea de código en
collector/forecaster/policy/action. La capacidad es un diferenciador IAM Enterprise: un sistema
que no sabe cuándo va a degradarse no puede garantizar SLOs de autenticación.

### Tareas de implementación

**Los 4 motores (2.02 — ningún archivo existe)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| V.1 | `internal/capacity/collector.go`: recolecta 30+ métricas cada 60s — Redis %, PG conexiones, K8s CPU/RAM por nodo, Kong RPS, bAuth cache miss. Escribe en `bos.cap_sistema_snapshot` (T-406) | `internal/capacity/collector.go` | `SELECT COUNT(*) FROM bos.cap_sistema_snapshot` → crece cada 60s |
| V.2 | `internal/capacity/forecaster.go`: regresión lineal sobre últimas N muestras, horizonte 7/30/90d, intervalo de confianza 80%/95% | `internal/capacity/forecaster.go` | `bosctl capacity forecast --horizon 30d` muestra proyección |
| V.3 | `internal/capacity/policy_engine.go`: lee `capacity.yml` declarativo, compara métricas vs thresholds, produce recomendaciones | `internal/capacity/policy_engine.go` | log "policy engine: Redis en WARN (75%)" cuando Redis > 70% |
| V.4 | `internal/capacity/action_engine.go`: escala HPA según política, emite alertas graduadas (INFO/WARN/CRIT), bloquea admisión de nuevos ctx_id si CRIT | `internal/capacity/action_engine.go` | `bos_rpc bos.capacity.check` → `{"status":"warn","blocked":false}` |
| V.5 | Wire los 4 motores en `run_normal.go` subsistema #16 (goroutine con ticker 60s) | `cmd/bos/run_normal.go` | log "autómata capacidad: ciclo 60s iniciado" al arrancar |
| V.6 | `bosctl capacity status`: tabla 30+ métricas con estado OK/WARN/CRIT y tendencia/h | `cmd/bosctl/` | `bosctl capacity status` muestra tabla no vacía |
| V.7 | `bosctl capacity forecast --horizon <7d|30d|90d>`: proyección con confianza por componente | `cmd/bosctl/` | `bosctl capacity forecast --horizon 7d` muestra proyección |
| V.8 | `bos.capacity.check/status/forecast` JSON-RPC: 3 métodos | `internal/server/jsonrpc.go` | `bos_rpc bos.capacity.status` → objeto con métricas |

**Watchdog Day 2 (2.01 · 2.04 Reconciliación)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| V.9 | Conectar watchdog con action_engine: degradación detectada → capacity policy evaluada → escala si corresponde | `internal/watchdog/` + `internal/capacity/action_engine.go` | ficha OOMKilled → watchdog detecta → action_engine escala → INSTALADA |
| V.10 | `bos.query.repair` con 8 threads reales (sin stubs): estado, health, drift, deps, historial, logs, ctx, política | `internal/server/query_repair.go` | `bos_rpc bos.query.repair {"ficha_id":"nextcloud"}` → causa raíz real |

**Escribir manual:** `2.05_MANUAL-SO-OBSERVABLE-CAPACIDAD-MOTORES.md` v1.0

### Verificación de la Fase V

```bash
bosctl capacity status
# Redis memory   45% [OK]  +0.2%/h  → WARN en 12d
# PG connections  8% [OK]  +0.1%/h  → WARN en 28d
# K8s CPU nodes  32% [OK]  estable

bosctl capacity forecast --horizon 30d
# Redis memory: 45% → 68% en 30d (conf 82%)  Estado: WARN proyectado día 12

# Métricas persistidas
PGPASSWORD=postgres psql -h localhost -p 15432 -U postgres -d SBOSDB \
  -c "SELECT COUNT(*) FROM bos.cap_sistema_snapshot"
# > 10 (al menos 10 ciclos de 60s)
```

---

## FASE VI — Dashboard certificado

**Motor:** ⑤ Dashboard · **Manuales:** 5.01 · 5.02 · 5.03 · 5.04
**Estándar IAM Enterprise:** JSON-RPC P99 < 150ms · WebSocket events · biaOS
**Requiere:** Fases I-V (todos los subsistemas deben funcionar para tener datos reales en el dashboard)

### Por qué sexto

El Dashboard es la interfaz que expone todo lo demás. El manual 5.01 lista 5 métodos
`bos.web.domain.*` sin código y el método `bos.capacity.forecast` incompleto. El agente IA
(`internal/biaos/`) tiene 4 métodos JSON-RPC pero no tiene manual propio. El Dashboard no puede
certificarse (P99 < 150ms) hasta que los subsistemas que consulta estén operativos.

### Tareas de implementación

**Métodos faltantes del catálogo (5.01 §2)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| VI.1 | `bos.web.domain.*`: add, remove, list, resolve, ssl_status — gestión de dominios web NGINX/Kong | `internal/server/jsonrpc.go` + `internal/domain/` | `bos_rpc bos.web.domain.list` → array de dominios |
| VI.2 | `bos.dashboard.*`: metrics, fichas, tenants, health, capacity — 5 métodos de agregación para UI | `internal/server/jsonrpc.go` | `bos_rpc bos.dashboard.health` → objeto con estado de todos los motores |
| VI.3 | `bos.capacity.forecast` JSON-RPC completo | `internal/server/jsonrpc.go` | `bos_rpc bos.capacity.forecast {"horizon":"30d"}` → proyección |

**biaOS — Agente IA soberano (5.05 nuevo manual)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| VI.4 | `internal/biaos/gateway.go`: completar integración con Ollama local (sin dependencia externa) | `internal/biaos/` | `bos_rpc bos.ai.ask {"prompt":"..."}` → respuesta desde Ollama |
| VI.5 | ICAP Engine: embedding de diagnosis → coseno en action_catalog.yml → acción sugerida con HITL | `internal/biaos/` | `bos_rpc bos.ai.diagnose {"ficha_id":"nextcloud"}` → causa + acción |

**Security RBAC (5.06 nuevo manual)**

| # | Tarea | Paquete / Archivo | Verificación |
|---|-------|-------------------|-------------|
| VI.6 | FileRBAC: verificar roles.json funcional para todos los métodos | `internal/security/` | `bosctl rbac list` muestra roles y permisos por método |
| VI.7 | BauthRBAC: activar verificación de bitmask bAuth en cada llamada JSON-RPC cuando bAuth disponible | `internal/security/` | llamada sin permisos → `-32003 Forbidden` |

**Escribir manuales:** `5.05_MANUAL-DASHBOARD-BIAOS.md` v1.0 +
`5.06_MANUAL-DASHBOARD-SECURITY-RBAC.md` v1.0 + Generar `RPC-CATALOG.md` con evidencia

### Verificación de la Fase VI

```bash
# 55+ métodos operativos
bos_rpc '{"jsonrpc":"2.0","method":"bos.dashboard.health","params":{},"id":1}'
# → {"result":{"installer":"ok","context":"ok","fichas":52,"capacity":"warn",...}}

bos_rpc '{"jsonrpc":"2.0","method":"bos.web.domain.list","params":{},"id":2}'
# → {"result":[{"domain":"skull.sbos.app","ssl":"active",...}]}

# biaOS operativo
bos_rpc '{"jsonrpc":"2.0","method":"bos.ai.diagnose",
  "params":{"ficha_id":"nextcloud","ctx_id":"sys"},"id":3}'
# → {"result":{"causa":"OOMKilled","accion":"SCALE_VERTICAL","confianza":0.91}}
```

---

## FASE VII — Certificación IAM Enterprise

**Motor:** ⑥ Banco de Pruebas · **Manual:** 6.01
**Estándar IAM Enterprise:** FAPI 2.0 · ISO 27001:2022 · NIST SP 800-63B · k6 SLOs
**Requiere:** Fases I-VI completas

### Por qué séptimo

La certificación no es el final del desarrollo — es la validación de que todo lo anterior
funciona a los estándares del sector. El manual 1.05 dice "Sin ejecutar" en cada punto de
estándares. El REGISTRO-ESTADO tiene M6.1-M6.5 todos 🔴. Sin certificación, el BOS puede
compilar y ejecutar pero no puede proclamarse IAM Enterprise.

### Tareas

**SLOs verificados con k6 (6.01 P-SLO-01..P-SLO-04)**

| # | SLO | Herramienta | Umbral |
|---|-----|-------------|--------|
| VII.1 | ctx_id lookup latencia | k6 Escenario Alpha (5 tenants, 50 RPS) | P99 < 5ms |
| VII.2 | JSON-RPC P99 | k6 Escenario Alpha | P99 < 150ms |
| VII.3 | Tenant deploy P50 | `time bosctl tenant create` × 10 muestras | P50 < 30s |
| VII.4 | Ficha install P50 | `time bosctl ficha install postgresql` × 10 | P50 < 60s |

**ISO 27001:2022 — 15 controles con evidencia (6.01 P-ISO-01..P-ISO-15)**

| # | Control | Evidencia requerida |
|---|---------|-------------------|
| VII.5 | A.8.15 Logging | `tail -100 /var/log/bos/audit.log` → ctx_id en cada línea |
| VII.6 | A.8.16 Monitoreo | Wazuh dashboard → alertas SBOS activas |
| VII.7 | A.8.20 Inventario activos | `bosctl port list` → Kardex T-408 con todos los activos de red |
| VII.8 | A.8.21 Seguridad red | `kubectl get networkpolicy -A` → deny-all en todos los namespaces |
| VII.9 | A.8.24 Gestión certs | `bosctl cert list` → todos los certs en Kardex T-413, ninguno expirado |
| VII.10 | A.9.4.2 TTL sesiones | ctx_id expira en 12h (verificar en Redis: `TTL bos:ctx:<id>`) |

**FAPI 2.0 (6.01 P-FAPI-01..P-FAPI-06)**

| # | Test | Herramienta | Resultado |
|---|------|-------------|-----------|
| VII.11 | PAR (Pushed Authorization Request) | FAPI Conformance Suite | PASS |
| VII.12 | DPoP (Demonstrating Proof-of-Possession) | FAPI Conformance Suite | PASS |
| VII.13 | PKCE obligatorio | FAPI Conformance Suite | PASS |
| VII.14 | Algoritmos PS256/ES256 únicos | FAPI Conformance Suite | PASS |

**SLSA L2 + SBOM**

| # | Tarea | Herramienta | Resultado |
|---|-------|-------------|-----------|
| VII.15 | Provenance Ed25519 en cada release | GitHub Actions + cosign | `cosign verify` → firma válida |
| VII.16 | SBOM CycloneDX JSON | `cyclonedx-gomod` | `bom.json` en cada release |

**Escribir:** `A.17_ANEXO-FAPI2-CERTIFICACION.md` v1.0 +
`INFORME-CERTIFICACION-IAM-ENTERPRISE-v1.0.md` (evidencia total)

### Verificación de la Fase VII

```bash
# SLOs
k6 run --vus 50 --duration 60s tests/k6/escenario-alpha.js
# http_req_duration p(99) < 150ms ✓
# ctx_id_lookup_p99 < 5ms        ✓

# FAPI 2.0 (staging con dominio público)
# Resultado: 0/X failures

# ISO 27001 evidencia
cat INFORME-CERTIFICACION-IAM-ENTERPRISE-v1.0.md | grep "✅" | wc -l
# 15 controles con evidencia ✓
```

---

## FASE VIII — VDI: la cúspide del SBOS

**Motor:** ① IAM Installer (capas 5-6) · **Referencia:** BOS-REPAIR-09 (SBOS-052) · BOS-REPAIR-14
**Estándar IAM Enterprise:** escritorio soberano · mTLS · SSO automático
**Requiere:** Fases I-VII completas

### Tareas

| # | Tarea | Archivo |
|---|-------|---------|
| VIII.1 | Ficha `nextcloud` + `onlyoffice`: S11-vdiserver completo | `servers/S11/` |
| VIII.2 | Ficha `guacamole`: proxy HTML5 para Fedora KDE accesible desde navegador | `servers/S11/` |
| VIII.3 | Ficha `fedora-logico`: escritorio Fedora KDE en K8s con home montado por tenant | `servers/S11/` |
| VIII.4 | `sbos-client`: binario WS mTLS :9444, SSO automático, modo kiosk | `cmd/sbos-client/` |
| VIII.5 | ISO SBOS: imagen booteable con sbos-client preinstalado y datos del tenant | CI pipeline |

### Verificación de la Fase VIII

```bash
# El criterio de la cúspide
curl -k https://skull.sbos.app/desktop
# → escritorio Fedora KDE accesible en navegador

./sbos-client --server https://skull.sbos.app
# → SSO automático → escritorio disponible
```

---

## Mapa de dependencias completo

```
FASE I — Stack Alpha (Cimientos)
    │
    ├──► FASE II — Zero Trust (Seguridad)
    │         │
    │         ├──► FASE III — Context Plane IAM (Identidad)
    │         │         │
    │         │         └──► (III es prerequisito para VI y VII)
    │         │
    │         └──► FASE IV — Server FICHAS wired (Fichas completas)
    │                   │
    │                   └──► (IV.11-IV.16 en paralelo con V y VI)
    │
    ├──► FASE V — Autómata Capacidad Day 2 (Observable)
    │
    └──► (I+II+III+IV+V necesarios para VI)
              │
              ▼
         FASE VI — Dashboard certificado
              │
              ▼
         FASE VII — Certificación IAM Enterprise
              │
              ▼
         FASE VIII — VDI: la cúspide ✦
```

---

## Corpus documental al completar las 8 fases

| Tipo | Existentes | A escribir | Total |
|------|:----------:|:----------:|:-----:|
| Manuales | 27 | 6 | 33 |
| Anexos | 13 | 3 | 16 |
| RPC-CATALOG | 0 | 1 | 1 |
| Informe certificación | 0 | 1 | 1 |

**Manuales a escribir en orden:**
1. `1.06_MANUAL-IAM-INSTALLER-ZERO-TRUST.md` → Fase II
2. `3.09_MANUAL-SERVER-FICHAS-EVENTBUS.md` → Fase IV
3. `4.06_MANUAL-CONTEXT-PLANE-IAM-ENTERPRISE.md` → Fase III
4. `2.05_MANUAL-SO-OBSERVABLE-CAPACIDAD-MOTORES.md` → Fase V
5. `5.05_MANUAL-DASHBOARD-BIAOS.md` → Fase VI
6. `5.06_MANUAL-DASHBOARD-SECURITY-RBAC.md` → Fase VI

**Anexos a escribir:**
- `A.16_ANEXO-ZERO-TRUST-IMPLEMENTACION.md` → Fase II
- `A.17_ANEXO-FAPI2-CERTIFICACION.md` → Fase VII

---

*BOS Control Plane Soberano · SKULL · SBOS · 2026-07-31*
*Versión 2.0.0 — basada en auditoría de 27 manuales + 18 docs bos-repair + código real*
*Norte: IAM Enterprise — FAPI 2.0 · NIST SP 800-207 · ISO 27001:2022 · OAuth 2.1*
