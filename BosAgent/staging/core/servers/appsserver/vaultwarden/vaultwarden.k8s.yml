---
apiVersion: v1
kind: Service
metadata:
  name: vaultwarden
  namespace: sbos-apps
spec:
  clusterIP: None
  selector:
    app: vaultwarden
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vaultwarden
  namespace: sbos-apps
spec:
  serviceName: vaultwarden
  replicas: 1
  selector:
    matchLabels:
      app: vaultwarden
  template:
    metadata:
      labels:
        app: vaultwarden
    spec:
      serviceAccountName: vaultwarden-sa
      containers:
        - name: vaultwarden
          image: docker.io/vaultwarden/server:1.32
          ports:
            - containerPort: 8080
              name: http

          volumeMounts:
            - name: data
              mountPath: /var/lib/vaultwarden
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10

  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: sbos-fast
        resources:
          requests:
            storage: 10Gi
