# yaml_engine.yml — wikijs lifecycle phases
# SBOS-049 §4.4 · Order · statefulset
# R16 Zero hardcoding

pre_install:
  - name: "create_namespace"
    command: "kubectl create namespace sbos-apps --dry-run=client -o yaml | kubectl apply -f -"
    timeout_seconds: 30
  - name: "apply_network_policy"
    command: "sbos_k8s_core apply -f ${SBOS_FICHA_DIR}/wikijs.network -n sbos-apps"
    timeout_seconds: 30

install:
  - name: "apply_manifest"
    command: "sbos_k8s_core apply -f ${SBOS_FICHA_DIR}/wikijs.k8s.yml -n sbos-apps"
    timeout_seconds: 300
  - name: "wait_ready"
    command: "kubectl wait --for=condition=Ready pod/wikijs-0 -n sbos-apps --timeout=600s"
    timeout_seconds: 600

post_install:
  - name: "verify"
    command: "echo 'verify_ok'"
    timeout_seconds: 30

update:
  - name: "rolling_restart"
    command: "kubectl rollout restart statefulset wikijs -n sbos-apps"
    timeout_seconds: 300

repair:
  - name: "restart"
    command: "kubectl rollout restart statefulset wikijs -n sbos-apps"
    timeout_seconds: 120

uninstall:
  - name: "delete_manifest"
    command: "kubectl delete statefulset wikijs -n sbos-apps --ignore-not-found"
    timeout_seconds: 120
  - name: "delete_pvc"
    command: "kubectl delete pvc wikijs-data -n sbos-apps --ignore-not-found"
    timeout_seconds: 60
