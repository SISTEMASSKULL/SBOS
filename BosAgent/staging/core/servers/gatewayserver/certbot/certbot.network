---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: certbot-netpol
  namespace: sbos-gateway
spec:
  podSelector:
    matchLabels:
      app: certbot
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              sbos-role: internal

      ports:
        - protocol: TCP
          port: 80
