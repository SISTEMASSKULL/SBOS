---
apiVersion: v1
kind: Service
metadata:
  name: cardmesh
  namespace: sbos-geo
spec:
  type: ClusterIP
  selector:
    app: cardmesh
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cardmesh
  namespace: sbos-geo
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: cardmesh
  template:
    metadata:
      labels:
        app: cardmesh
    spec:
      serviceAccountName: cardmesh-sa
      containers:
        - name: cardmesh
          image: skull/cardmesh:latest
          ports:
            - containerPort: 8080
              name: http

          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10


