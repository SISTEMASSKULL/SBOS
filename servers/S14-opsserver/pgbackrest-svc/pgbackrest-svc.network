---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: pgbackrest-svc-netpol
  namespace: sbos-ops
spec:
  podSelector:
    matchLabels:
      app: pgbackrest-svc
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              sbos-role: internal

      ports:
        - protocol: TCP
          port: 8080
