---
apiVersion: v1
kind: Service
metadata:
  name: smartrates
  namespace: sbos-apps
spec:
  type: ClusterIP
  selector:
    app: smartrates
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartrates
  namespace: sbos-apps
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: smartrates
  template:
    metadata:
      labels:
        app: smartrates
    spec:
      serviceAccountName: smartrates-sa
      containers:
        - name: smartrates
          image: skull/smartrates:latest
          ports:
            - containerPort: 8080
              name: http

          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10


